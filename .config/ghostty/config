## See https://ghostty.org/docs/config for official docs & option 
## sets or run `ghostty +list-configuration --default` 

## Appearance
theme = catppuccin-mocha
window-colorspace = display-p3
split-divider-color = #585b70
macos-titlebar-proxy-icon = hidden
title = " "
# backup of mocha
# config-file = ?./catppuccin-mocha

## Fonts
font-family = CommitMono Nerd Font
font-feature = +cv07
font-size = 16
font-thicken = true
adjust-cell-height = 15%

## Default Window Dimensions
window-height = 35
window-width = 150

## Splits
focus-follows-mouse = true

## Misc
copy-on-select = true
confirm-close-surface = false
quit-after-last-window-closed = true
window-save-state = default

## Keybinds
# fix tmux-vim-navigate
keybind = alt+right=unbind
keybind = alt+left=unbind

## tmux-ify keybinds
# tmux windows (tabs in Ghostty)
keybind = "ctrl+space>c=new_tab"
keybind = "ctrl+space>n=next_tab"
#TODO: previous tab (index -1), Ghostty does not have this action yet
keybind = "ctrl+space>l=previous_tab"
#TODO: navigate tabs by index via goto_tab:{index} without writing every possible index

# tmux panes (splits in Ghostty)
# keybind = "ctrl+space>shift+5=new_split:right"
#TODO: new_split:down (quote is not a char available for mapping)
# keybind = "ctrl+space>right=goto_split:right"
# keybind = "ctrl+space>left=goto_split:left"
# keybind = "ctrl+space>up=goto_split:up"
# keybind = "ctrl+space>down=goto_split:down"
# keybind = "ctrl+space>o=goto_split:next"
# keybind = "ctrl+space>semicolon=goto_split:previous"

## tmux-vim-navigate-ify splits
# keybind = alt+h=goto_split:left
# keybind = alt+j=goto_split:down
# keybind = alt+k=goto_split:up
# keybind = alt+l=goto_split:right
# keybind = alt+slash=goto_split:previous

## comfort navigate splits
# keybind = alt+left=goto_split:left
# keybind = alt+down=goto_split:down
# keybind = alt+up=goto_split:up
# keybind = alt+right=goto_split:right

## Scrollback Hack (open scrollback in $EDITOR)
# pressing ctrl+f will pipe the scrollback buffer into a
# file and paste the filepath into the prompt
keybind = ctrl+f=write_scrollback_file:paste
# now press super+f to open in your editor!
keybind = "super+f=text:\x1b[H ${EDITOR} \n"

